TYPE construction_graph
INPUT 0
BODY
	create_graph urtree g 0
	assert num_nodes g 0
	assert num_edges g 0
	assert is_tree g
	assert is_rooted g
	assert not_has_root g
	set_root g 1
	assert has_root g
	
	create_graph urtree g 1
	assert num_nodes g 1
	assert num_edges g 0
	assert is_rooted g
	assert is_tree g
	
	create_graph urtree g 2
	assert num_nodes g 2
	assert num_edges g 0
	assert is_rooted g
	assert is_not_tree g
	
	create_graph urtree g 3
	assert num_nodes g 3
	assert num_edges g 0
	assert is_rooted g
	assert is_not_tree g
	
	create_graph urtree g 4
	assert num_nodes g 4
	assert num_edges g 0
	assert is_rooted g
	assert is_not_tree g
	
	create_graph urtree g 5
	assert num_nodes g 5
	assert num_edges g 0
	assert is_rooted g
	assert is_not_tree g
