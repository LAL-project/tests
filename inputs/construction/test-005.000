TYPE construction_graph
INPUT 0
BODY
	create_graph drtree g 0
	assert num_nodes g 0
	assert num_edges g 0
	assert is_rooted g
	assert is_tree g
	assert num_verts_subtrees_not_valid g
	assert drtree_type_not_valid g
	
	create_graph drtree g 1
	assert num_nodes g 1
	assert num_edges g 0
	assert is_rooted g
	assert is_tree g
	assert num_verts_subtrees_not_valid g
	assert drtree_type_not_valid g
	
	create_graph drtree g 2
	assert num_nodes g 2
	assert num_edges g 0
	assert is_rooted g
	assert is_not_tree g
	assert num_verts_subtrees_not_valid g
	assert drtree_type_not_valid g
	
	create_graph drtree g 3
	assert num_nodes g 3
	assert num_edges g 0
	assert is_rooted g
	assert is_not_tree g
	assert num_verts_subtrees_not_valid g
	assert drtree_type_not_valid g
	
	create_graph drtree g 4
	assert num_nodes g 4
	assert num_edges g 0
	assert is_rooted g
	assert is_not_tree g
	assert num_verts_subtrees_not_valid g
	assert drtree_type_not_valid g
	
	create_graph drtree g 5
	assert num_nodes g 5
	assert num_edges g 0
	assert is_rooted g
	assert is_not_tree g
	assert num_verts_subtrees_not_valid g
	assert drtree_type_not_valid g
