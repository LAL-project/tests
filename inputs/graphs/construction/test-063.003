TYPE graphs-construction
INPUT 0
BODY
	create_graph rtree h 11
	add_edges h 10
		1 3
		5 1
		6 5
		7 6
		0 1
		2 0
		4 2
		8 4
		9 4
		10 4
		false true
	assert rtree_orientation_not_valid h
	assert not_has_root h
	set_root h 3
	assert has_root h
	assert is_tree h
	find_edge_orientation h
	assert rtree_orientation_not_valid h

	/*
	we can't do "calculate_size_subtrees h" because we don't have
	a rtree type
	*/

	set_root h 0
	/*
	we can't do "calculate_size_subtrees h" because we don't have
	a rtree type
	*/
	assert rtree_orientation_not_valid h
	find_edge_orientation h
	assert rtree_orientation_not_valid h
	/*
	we still can't do "calculate_size_subtrees h" because the rtree
	type is not an arborescence
	*/
